{% extends 'base.html.twig' %}

{% block title %}Scans{% endblock %}

{% block body %}
    <h1>Scans</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ scan.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ scan.code }}</td>
            </tr>
            <tr>
                <th>Scanner_ip</th>
                <td>{{ scan.scannerIp }}</td>
            </tr>
            <tr>
                <th>Created_date</th>
                <td>{{ scan.createdDate ? scan.createdDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('scans_index') }}">back to list</a>

    <a href="{{ path('scans_edit', {'id': scan.id}) }}">edit</a>

    {{ include('scans/_delete_form.html.twig') }}
{% endblock %}
